---
title: "MediaDeviceInfo: kind property"
short-title: kind
slug: Web/API/MediaDeviceInfo/kind
page-type: web-api-instance-property
browser-compat: api.MediaDeviceInfo.kind
---

{{APIRef("Media Capture and Streams")}}{{securecontext_header}}

خاصیت فقط خواندنی **`kind`** از رابط {{domxref("MediaDeviceInfo")}} یک مقدار شمارشی برمی‌گرداند که می‌تواند `"videoinput"`، `"audioinput"` یا `"audiooutput"` باشد.

## مقدار

یکی از `"videoinput"`، `"audioinput"` یا `"audiooutput"`.

## مشخصات

{{Specifications}}

## سازگاری با مرورگر

{{Compat}}